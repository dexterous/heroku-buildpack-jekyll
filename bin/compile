#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

generate_gemfile() {
cat << EOF > $1/Gemfile
source :rubygems
gem "jekyll"
EOF
}

generate_config_ru() {
cat << EOF > $1/config.ru
require "rack/static"
use Rack::Static, urls: ["/"], root: "_site", index: "index.html", headers: { "Cache-Control" => "public" }
run ->(env) { [ 404, {}, [] ] }
EOF
}

echo "-----> Installing dependencies"
if [ ! -f $1/Gemfile ]; then
  generate_gemfile
fi
gem install bundler --install-dir $1/.gems | indent
bundle install --deployment | indent

echo "-----> Compiling Jekyll"
bundle exec jekyll --no-auto --no-server | indent

echo "-----> Adding config.ru"
generate_config_ru
